{
  "version": 2,
  "buildCommand": "cd client && npm install --include=dev && node_modules/.bin/vite build",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'skip'",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
