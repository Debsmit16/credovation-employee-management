{
  "version": 2,
  "installCommand": "npm install --prefix server && cd server && npx prisma generate --schema=src/prisma/schema.prisma && cd .. && cd client && npm install --include=dev && cd ..",
  "buildCommand": "cd client && node_modules/.bin/vite build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "server/**"
    }
  }
}
